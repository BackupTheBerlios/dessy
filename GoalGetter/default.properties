# -------------------------------------------------------------
# DEFAULT GOALGETTER BUILD PROPERTIES
# -------------------------------------------------------------
#
# DO NOT EDIT THIS FILE IN ORDER TO CUSTOMIZE BUILD PROPERTIES.
# CREATE AND EDIT build.properties FILE INSTEAD.
#

# Project Properties ------------------------------------------
Name=GoalGetter
Shortname=GoalGetter
name=goalgetter
spec.version=0.1
impl.version=0.1
dist.version=0.1-20040927
main.class=de.uniessen.wiinf.wip.goalgetter.GoalGetter
copyright.date=2004
copyright.owner=Jonas Sprenger and Tim Franz
copyright.message=Copyright &#169; ${copyright.date} ${copyright.owner}. All Rights Reserved. 

top.dir=${basedir}


# Source Properties -------------------------------------------
src.dir          = ${top.dir}/src
src.main.dir     = ${src.dir}/main
src.resources.dir= ${src.dir}/resources
docs.dir         = ${top.dir}/docs


# Library Properties ------------------------------------------
lib.dir = ${top.dir}/lib
lib.apple.jar   = ${lib.dir}/apple-extensions.jar
lib.binding.jar = ${lib.dir}/binding-1.0b2.jar
lib.forms.jar   = ${lib.dir}/forms-1.0.5-snapshot.jar
lib.looks.jar   = ${lib.dir}/looks-1.3-snapshot.jar
lib.uif.jar     = ${lib.dir}/uif-1.4-snapshot.jar
lib.uif-extras.jar = ${lib.dir}/uif-extras-1.4-snapshot.jar
lib.validation.jar = ${lib.dir}/validation-0.9.5.jar
lib.gnujaxp.jar = ${lib.dir}/gnujaxp.jar
lib.jcommon.jar = ${lib.dir}/jcommon-0.9.6.jar
lib.jfreechart.jar = ${lib.dir}/jfreechart-0.9.21.jar
lib.servlet.jar = ${lib.dir}/servlet.jar


# JavaDoc Properties ------------------------------------------
javadoc.link=http://java.sun.com/j2se/1.4/docs/api/
javadoc.packages=de.uniessen.wiinf.wip.goalgetter.*


# Build Properties --------------------------------------------
build.compiler.pedantic=false
build.compile.debug=on
build.compile.deprecation=off
build.compile.fork=no
build.compile.nowarn=on
build.compile.target=1.3
build.encoding=ISO-8859-1

build.dir          = ${top.dir}/build
build.classes.dir  = ${build.dir}/classes
build.main.dir     = ${build.classes.dir}/main
build.docs.dir     = ${build.dir}/docs
build.javadocs.dir = ${build.docs.dir}/api
build.reports.dir  = ${build.dir}/test-reports

build.manifest     = ${build.dir}/MANIFEST.MF
build.main.jar     = ${build.dir}/${name}.jar



# Dist Properties -----------------------------------------------
dist.root.dir        = ${top.dir}/dist
dist.name     	     = ${name}-${impl.version}
dist.name.mac        = ${Shortname} ${spec.version}
deploy.name          = ${name}-${dist.version}

dist.source.subdir   = ${dist.name}-src
dist.source.dir      = ${dist.root.dir}/${dist.source.subdir}
dist.source.docs.dir = ${dist.source.dir}/docs
dist.source.lib.dir  = ${dist.source.dir}/lib
dist.source.src.dir  = ${dist.source.dir}/src
dist.source.main.jar = ${dist.source.dir}/${dist.name}.jar
dist.source.zip      = ${dist.root.dir}/${deploy.name}-src.zip

dist.binary.subdir   = ${dist.name}
dist.binary.dir      = ${dist.root.dir}/${dist.binary.subdir}
dist.binary.main.jar = ${dist.binary.dir}/${dist.name}.jar
dist.binary.zip      = ${dist.root.dir}/${deploy.name}.zip

dist.jnlp.subdir     = ${dist.name}-jnlp
dist.jnlp.dir        = ${dist.root.dir}/${dist.jnlp.subdir}
dist.jnlp.main.jar   = ${dist.jnlp.dir}/${name}.jar


